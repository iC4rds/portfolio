# wrangler.toml
name = "portfolio"
compatibility_date = "2024-01-01"

# Kein [site] Eintrag! Das ist veraltet
# Stattdessen für SvelteKit:
main = "./.svelte-kit/cloudflare/_worker.js"

# Environment Variablen für die Laufzeit
[vars]
NODE_VERSION = "20"

# Production-Umgebung
[env.production]
vars = { NODE_VERSION = "20" }